public class Subscriber implements Observer {
    private String subName;

    public Subscriber(String name) {
        this.subName = subName;
    }

    public void update(String news) {
        System.out.println(subName + "received the news: " + news);
    }
}
